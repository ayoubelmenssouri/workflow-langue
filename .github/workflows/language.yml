name: "workflow langue"
on: 
  push: 
    branches : 
      - "lang"
jobs:
  fr-job:
    if : vars.LANG == 'FR'
    runs-on: ubuntu-latest
    steps:
      - name: Afficher un message
        run: echo "L'utilisateur ${{github.actor}} parle français"

      
  second-job:
    if : vars.LANG != 'FR'
    runs-on: macos-latest
    steps:
      - name: Afficher un message
        run: echo "user ${{github.actor}} speaks english1"